services:
  arbeitszeit:
    build: .
    container_name: arbeitszeit
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - TZ=Europe/Berlin
    volumes:
      - ./data:/app/data
