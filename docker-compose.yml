services:
  arbeitszeit:
    build: .
    container_name: arbeitszeit
    restart: unless-stopped
    ports:
      - "7536:3000"
    environment:
      - TZ=Europe/Berlin
    volumes:
      - ./data:/app/data
